% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population_stratification.R
\name{simulate_population_stratified}
\alias{simulate_population_stratified}
\title{Simulate a population with stratification using Balding-Nichols model}
\usage{
simulate_population_stratified(
  n_individuals,
  n_snps = NULL,
  population,
  ancestral_af = NULL,
  Fst = NULL,
  inbreeding_F = 0,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{n_individuals}{Integer. Number of diploid individuals to simulate.}

\item{n_snps}{Integer. Number of SNPs (if `ancestral_af` is not provided).}

\item{population}{Character. Population name (must be one of those returned
by \code{\link{reference_populations}}).}

\item{ancestral_af}{Either a numeric vector of length `n_snps` or a file path
(passed to \code{\link{load_af_from_file}}). If `NULL`, uniform random
frequencies are used as ancestral (not recommended).}

\item{Fst}{Override the population-specific Fst. If `NULL`, uses the value
from \code{\link{reference_populations}}.}

\item{inbreeding_F}{Numeric in [0,1]. Inbreeding coefficient for all
individuals (applied via \code{.sample_genotypes_vec}). Default 0.}

\item{seed}{Random seed (optional).}

\item{verbose}{Logical. Print progress.}
}
\value{
A genotype matrix (individuals x SNPs) with dosages 0,1,2.
}
\description{
Generates a genotype matrix for a single population where allele frequencies
are drawn from a Beta distribution parameterised by ancestral frequencies
and population-specific Fst. This implements the Balding-Nichols model.
}
\details{
The Balding-Nichols model draws population-specific allele frequency
\eqn{p_{pop}} for each SNP as:
\deqn{p_{pop} \sim \text{Beta}(\alpha, \beta)}
where \eqn{\alpha = p_{anc} \cdot (1-F_{st}) / F_{st}} and
\eqn{\beta = (1-p_{anc}) \cdot (1-F_{st}) / F_{st}}.
Then genotypes are sampled under Hardy‑Weinberg equilibrium (or with
inbreeding if `inbreeding_F > 0`).
}
\examples{
\dontrun{
# Simulate 100 individuals from South Asian population using gnomAD ancestral AFs
anc <- load_af_from_file("gnomad_global_af.csv")
geno <- simulate_population_stratified(
  n_individuals = 100,
  ancestral_af = anc,
  population = "SAS",
  seed = 123
)
}

}
