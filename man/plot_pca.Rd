% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{plot_pca}
\alias{plot_pca}
\title{Plot PCA of Simulated Genotypes}
\usage{
plot_pca(pca_result, color_by = NULL, out_file = NULL)
}
\arguments{
\item{pca_result}{List returned by \code{\link{run_pca}}.}

\item{color_by}{Character vector of length \eqn{n_{individuals}} for
colour grouping (e.g. generation labels). Default \code{NULL}.}

\item{out_file}{Character or \code{NULL}. PDF output path.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Scatter plot of PC1 vs PC2 from a \code{\link{run_pca}} result, with
optional colour grouping.
}
\examples{
sim <- simulate_population(n_founders=100, n_snps=300, n_generations=3, seed=1)
all_g <- do.call(rbind, sim$genotypes)
pca   <- run_pca(all_g)
gen_labels <- sub("_ind.*","", rownames(pca$scores))
plot_pca(pca, color_by = gen_labels)
}
\seealso{
\code{\link{run_pca}}
}
